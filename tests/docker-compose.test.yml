version: '3.8'

services:
  powershell-tests:
    build:
      context: ..
      dockerfile: tests/Dockerfile.test
    container_name: archive-tovarishestvo-tests
    volumes:
      - ..:/app
    working_dir: /app
    command: pwsh -Command "Invoke-Pester ./tests/E2E-ImageProcessing.Tests.ps1 -Output Detailed"
